{
  "name": "axon-backend",
  "version": "0.0.0",
  "private": true,
  "description": "The mastery-* Cloudflare Workers pipeline for Axon: triage, structure, content, reconcile, adjudicate, explain, sweep and the student-facing api worker, plus the shared library between them.",
  "workspaces": [
    "shared",
    "workers/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check:contract-parity": "node scripts/check-cross-repo-contract.mjs",
    "dry-run": "for w in workers/*; do (cd \"$w\" && npx wrangler deploy --dry-run) || exit 1; done",
    "test:registry": "node --test scripts/lib/*.test.mjs",
    "schemes:cbse": "node scripts/cbse-scheme-registry.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.0",
    "@types/node": "^26.4.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.127.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.0"
  }
}
